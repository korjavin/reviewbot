# ── ReviewBot (GitHub App) ────────────────────────
HOSTNAME=reviewbot.example.com
GITHUB_APP_ID=123456
GITHUB_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----...
GITHUB_WEBHOOK_SECRET=your-webhook-secret
GITHUB_CLIENT_ID=Iv1.xxxxxxxx
GITHUB_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxx
PORT=8080
BASE_URL=https://reviewbot.example.com

# Webhook URL of the n8n inbox_handler workflow.
# Set after importing inbox_handler.json into n8n.
N8N_WEBHOOK_URL=https://n8n.example.com/webhook/reviewbot-inbox

# ── n8n ───────────────────────────────────────────
N8N_HOSTNAME=n8n.example.com
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme

# ── AnythingLLM ───────────────────────────────────
ANYTHINGLLM_HOSTNAME=kb.example.com
# Get from AnythingLLM UI → Settings → API Keys
ANYTHINGLLM_API_KEY=

# ── LLM Provider (shared) ─────────────────────────
# gemini | claude | openai | ollama
LLM_PROVIDER=gemini
LLM_MODEL=gemini-1.5-flash
LLM_API_KEY=
# Used directly by claude-runner (Anthropic API key for claude CLI)
ANTHROPIC_API_KEY=

# ── Intel markdown files (host path) ──────────────
# Folder with .md intel files, mounted into AnythingLLM and watched by kb-maintainer
INTELS_DIR=./intels

# ── KB Maintainer ─────────────────────────────────
# AnythingLLM workspace slug to sync intels into (created automatically if absent)
ANYTHINGLLM_WORKSPACE=intels
# How often to do a full resync in addition to inotify-driven events (Go duration)
KB_SYNC_INTERVAL=5m

# ── Infrastructure ────────────────────────────────
NETWORK_NAME=traefik_network

# ── Development / Testing ─────────────────────────
# GitHub personal access token for the claude-runner-test.json workflow
GITHUB_TEST_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
